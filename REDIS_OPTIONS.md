# Бесплатные варианты Redis для Django Channels

## 🚨 Важно: InMemory vs Redis

**Текущая конфигурация:** InMemoryChannelLayer (без Redis)
- ✅ Работает на одном worker (Render.com free tier)
- ✅ Подходит для минимальной версии
- ⚠️ НЕ работает если несколько workers
- ⚠️ Теряет подключения при рестарте сервера

**С Redis:**
- ✅ Поддержка нескольких workers
- ✅ Надежное хранение подключений
- ✅ Масштабируемость

## 🆓 Бесплатные Redis сервисы

### 1. Upstash Redis (РЕКОМЕНДУЮ)
**Почему лучший:**
- 10,000 команд в день бесплатно
- Очень быстрый
- Легкая настройка

**Как подключить:**
1. Зарегистрируйтесь: https://upstash.com/
2. Create Redis Database
3. Копируйте Redis URL (формат: `rediss://...`)
4. Добавьте на Render.com environment variable:
   ```
   REDIS_URL=rediss://default:xxxxxx@xxxxxx.upstash.io:6379
   ```

### 2. Redis Cloud (RedisLabs)
**Бесплатный план:**
- 30 MB памяти
- Надежный сервис

**Как подключить:**
1. Зарегистрируйтесь: https://redis.com/try-free/
2. Create Database
3. Получите Redis URL
4. Добавьте на Render: `REDIS_URL=redis://...`

### 3. Railway
**Бесплатный план:**
- $5 кредитов каждый месяц
- Простая настройка

**Как подключить:**
1. Зарегистрируйтесь: https://railway.app/
2. New Project → Add Redis
3. Копируйте REDIS_URL из переменных
4. Добавьте на Render

## ⚙️ Настройка на Render.com

После получения Redis URL:

1. Откройте Render Dashboard
2. Ваш сервис → Environment
3. Добавьте переменную:
   ```
   REDIS_URL=<ваш-redis-url>
   ```
4. Save Changes (сервис автоматически перезапустится)

## 🧪 Проверка подключения

После добавления REDIS_URL, проверьте логи Render:
```
Dashboard → Your Service → Logs
```

Должны увидеть:
```
[INFO] Using Redis channel layer
```

Вместо:
```
[INFO] Using InMemory channel layer
```

## 🎯 Для минимальной версии

**Можете оставить как есть (InMemory):**
- Работает для 1-1 звонков
- Free tier Render использует 1 worker
- InMemory достаточно

**Когда нужен Redis:**
- Планируете масштабировать (больше workers)
- Нужна надежность соединений
- Планируете групповые звонки (3+ человек)

## 📝 Текущая конфигурация

Ваше приложение уже настроено:
- Если `REDIS_URL` установлен → использует Redis
- Если нет → использует InMemory

Просто добавьте переменную когда будете готовы!
